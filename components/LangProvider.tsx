'use client'

import { ReactNode } from 'react'

export function LangProvider({ children }: { children: ReactNode }) {
  // This provider is a placeholder for any future language context needs
  // The useLang hook handles everything on its own
  return <>{children}</>
}
